<Window x:Class="jxsUI.Windows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:jxsUI"
        mc:Ignorable="d"
        Title="{Binding Title}" Height="{Binding  MainHeight, Mode=TwoWay}" Width="{Binding MainWidth, Mode=TwoWay}"
        Top="{Binding MainTop, Mode=TwoWay }" Left="{Binding MainLeft,Mode=TwoWay}">
    <Grid>
        
    </Grid>
</Window>
